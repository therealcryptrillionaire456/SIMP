apiVersion: v1
kind: Service
metadata:
  name: projectx-runtime
  labels:
    app: projectx-runtime
spec:
  selector:
    app: projectx-runtime
  ports:
    - name: http
      port: 80
      targetPort: 5555
  type: ClusterIP
